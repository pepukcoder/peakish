# Gear Articles

Each file here is a standalone transparent HTML article intended for iframe/wiki embedding.

Structure per file:
- `article.wiki-article`
- `h1` title
- optional `img`
- paragraph-only content block

All article pages use transparent `html, body` backgrounds.
